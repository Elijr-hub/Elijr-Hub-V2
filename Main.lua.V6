-- Elijr Hub – Steal a Brainrot Edition (V6 COMPLETE)
local fenv = getfenv()
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local VelocityConnection, FlyConnection
local Enabled, FlyEnabled, AntiKB, AntiRagdoll, SpinEnabled, XrayEnabled, AutoSteal = false, false, false, false, false, false, false
local Speed, JumpPower, SpinSpeed, FlySpeed, FOVValue, StealDist = 30, 75, 50, 15, 70, 50 

-- ────────────────────────────────────────────────────────────────
--                         CORE LOGIC
-- ────────────────────────────────────────────────────────────────

local ScreenGui = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
ScreenGui.Name = "ElijrHubV6"
ScreenGui.ResetOnSpawn = false

-- PROGRESS BAR (Black & Yellow)
local ProgressBack = Instance.new("Frame", ScreenGui)
ProgressBack.Size = UDim2.new(0, 300, 0, 25)
ProgressBack.Position = UDim2.new(0.5, -150, 0, 20)
ProgressBack.BackgroundColor3 = Color3.new(0,0,0)
ProgressBack.Visible = false
Instance.new("UICorner", ProgressBack).CornerRadius = UDim.new(0, 8)
local ProgStroke = Instance.new("UIStroke", ProgressBack)
ProgStroke.Color = Color3.new(1,1,0)
ProgStroke.Thickness = 2

local ProgressFill = Instance.new("Frame", ProgressBack)
ProgressFill.Size = UDim2.new(0, 0, 1, 0)
ProgressFill.BackgroundColor3 = Color3.new(1,1,0)
Instance.new("UICorner", ProgressFill).CornerRadius = UDim.new(0, 8)

-- AUTO STEAL LOOP (3 Seconds)
task.spawn(function()
    while task.wait(0.1) do
        if AutoSteal then
            local Char = LocalPlayer.Character
            local HRP = Char and Char:FindFirstChild("HumanoidRootPart")
            if HRP then
                local target = nil
                for _, v in pairs(workspace:GetDescendants()) do
                    if v:IsA("TouchTransmitter") and v.Parent and v.Parent:IsA("BasePart") then
                        if (HRP.Position - v.Parent.Position).Magnitude <= StealDist then
                            target = v.Parent break
                        end
                    end
                end
                if target then
                    ProgressBack.Visible = true
                    ProgressFill.Size = UDim2.new(0,0,1,0)
                    ProgressFill:TweenSize(UDim2.new(1, 0, 1, 0), "Out", "Linear", 3)
                    task.wait(3)
                    if AutoSteal and target and target.Parent and (HRP.Position - target.Position).Magnitude <= StealDist + 5 then
                        firetouchinterest(HRP, target, 0)
                        firetouchinterest(HRP, target, 1)
                    end
                    ProgressBack.Visible = false
                    ProgressFill.Size = UDim2.new(0,0,1,0)
                end
            end
        end
    end
end)

-- FLY & SPIN LOGIC
local function ToggleFly(state)
    if FlyConnection then FlyConnection:Disconnect() FlyConnection = nil end
    local HRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local Hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if state and HRP and Hum then
        local BV = Instance.new("BodyVelocity", HRP) BV.Name = "ElijrFly"
        BV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        local BG = Instance.new("BodyGyro", HRP) BG.Name = "ElijrGyro"
        BG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        Hum.PlatformStand = true
        FlyConnection = RunService.RenderStepped:Connect(function()
            BG.CFrame = Camera.CFrame
            if Hum.MoveDirection.Magnitude > 0 then
                BV.Velocity = (Camera.CFrame.Rotation * Vector3.new(Hum.MoveDirection.X, 0, -Hum.MoveDirection.Magnitude)).Unit * FlySpeed
                BV.Velocity = Vector3.new(BV.Velocity.X, Camera.CFrame.LookVector.Y * FlySpeed * Hum.MoveDirection.Magnitude, BV.Velocity.Z)
            else BV.Velocity = Vector3.new(0, 0.1, 0) end
        end)
    else
        if HRP then if HRP:FindFirstChild("ElijrFly") then HRP.ElijrFly:Destroy() end if HRP:FindFirstChild("ElijrGyro") then HRP.ElijrGyro:Destroy() end end
        if Hum then Hum.PlatformStand = false end
    end
end

local function ToggleSpin(state)
    local HRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if state and HRP then
        local Spin = Instance.new("AngularVelocity", HRP) Spin.Name = "ElijrSpin"
        Spin.MaxTorque = math.huge
        Spin.AngularVelocity = Vector3.new(0, SpinSpeed, 0)
        Spin.Attachment0 = HRP:FindFirstChildOfClass("Attachment") or Instance.new("Attachment", HRP)
    else
        if HRP and HRP:FindFirstChild("ElijrSpin") then HRP.ElijrSpin:Destroy() end
    end
end

RunService.Heartbeat:Connect(function()
    local Char = LocalPlayer.Character
    local HRP = Char and Char:FindFirstChild("HumanoidRootPart")
    local Hum = Char and Char:FindFirstChildOfClass("Humanoid")
    if HRP and Hum and Enabled and not FlyEnabled then
        if Hum.MoveDirection.Magnitude > 0 then
            HRP.Velocity = Vector3.new(Hum.MoveDirection.Unit.X * Speed, HRP.Velocity.Y, Hum.MoveDirection.Unit.Z * Speed)
        end
    end
    Camera.FieldOfView = FOVValue
end)

-- ────────────────────────────────────────────────────────────────
--                           GUI SETUP
-- ────────────────────────────────────────────────────────────────

local OpenBtn = Instance.new("TextButton", ScreenGui)
OpenBtn.Size = UDim2.new(0, 60, 0, 60)
OpenBtn.Position = UDim2.new(0, 15, 0.5, -30)
OpenBtn.BackgroundColor3 = Color3.new(0,0,0)
OpenBtn.Text = "<b>OPEN</b>"
OpenBtn.TextColor3 = Color3.new(1,1,0)
OpenBtn.RichText = true
OpenBtn.Font = Enum.Font.FredokaOne
OpenBtn.Visible = false
OpenBtn.Draggable = true
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", OpenBtn).Color = Color3.new(1,1,0)

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 420, 0, 320)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -160)
MainFrame.BackgroundColor3 = Color3.new(0,0,0)
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 15)
Instance.new("UIStroke", MainFrame).Color = Color3.new(1,1,0)

local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -40, 0, 5)
CloseBtn.Text = "<b>X</b>"
CloseBtn.TextColor3 = Color3.new(1,1,0)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Font = Enum.Font.FredokaOne
CloseBtn.TextSize = 25
CloseBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false OpenBtn.Visible = true end)
OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = true OpenBtn.Visible = false end)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 45)
Title.Text = "Elijr Hub"
Title.TextColor3 = Color3.new(1,1,0)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.FredokaOne
Title.TextSize = 28

local LeftNav = Instance.new("Frame", MainFrame)
LeftNav.Size = UDim2.new(0, 110, 1, -60)
LeftNav.Position = UDim2.new(0, 10, 0, 50)
LeftNav.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Instance.new("UICorner", LeftNav)
Instance.new("UIStroke", LeftNav).Color = Color3.new(1,1,0)

local ContentArea = Instance.new("Frame", MainFrame)
ContentArea.Size = UDim2.new(1, -140, 1, -60)
ContentArea.Position = UDim2.new(0, 130, 0, 50)
ContentArea.BackgroundTransparency = 1

local Tabs = {}
local function CreateTab(name)
    local F = Instance.new("ScrollingFrame", ContentArea)
    F.Size = UDim2.new(1, 0, 1, 0)
    F.BackgroundTransparency = 1
    F.Visible = false
    F.ScrollBarThickness = 0
    Tabs[name] = F
    return F
end

local MoveTab, SpinTab, ViewTab, MiscTab = CreateTab("Move"), CreateTab("Spin"), CreateTab("View"), CreateTab("Misc")

local function ShowTab(name) for n, f in pairs(Tabs) do f.Visible = (n == name) end end

local function CreateNavBtn(name, y)
    local B = Instance.new("TextButton", LeftNav)
    B.Size = UDim2.new(1, -10, 0, 45)
    B.Position = UDim2.new(0, 5, 0, y)
    B.Text = "<b>" .. name:upper() .. "</b>"
    B.RichText = true
    B.Font = Enum.Font.FredokaOne
    B.TextColor3 = Color3.new(1,1,0)
    B.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Instance.new("UICorner", B)
    B.MouseButton1Click:Connect(function() ShowTab(name) end)
end

CreateNavBtn("Move", 10) CreateNavBtn("Spin", 65) CreateNavBtn("View", 120) CreateNavBtn("Misc", 175)

local function CreateToggle(parent, text, y, callback)
    local B = Instance.new("TextButton", parent)
    B.Size = UDim2.new(1, -10, 0, 40)
    B.Position = UDim2.new(0, 5, 0, y)
    B.Text = "<b>" .. text .. "</b>"
    B.RichText = true
    B.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    B.TextColor3 = Color3.new(1,1,0)
    B.Font = Enum.Font.FredokaOne
    Instance.new("UICorner", B)
    local s = false
    B.MouseButton1Click:Connect(function()
        s = not s callback(s)
        B.BackgroundColor3 = s and Color3.new(1,1,0) or Color3.fromRGB(20, 20, 20)
        B.TextColor3 = s and Color3.new(0,0,0) or Color3.new(1,1,0)
    end)
end

local function CreateInput(parent, text, y, def, callback)
    local T = Instance.new("TextBox", parent)
    T.Size = UDim2.new(1, -10, 0, 35)
    T.Position = UDim2.new(0, 5, 0, y)
    T.Text = "<b>" .. text .. def .. "</b>"
    T.RichText = true
    T.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    T.TextColor3 = Color3.new(1,1,0)
    T.Font = Enum.Font.FredokaOne
    Instance.new("UICorner", T)
    T.FocusLost:Connect(function() 
        local val = tonumber(T.Text:match("%d+"))
        if val then callback(val) T.Text = "<b>"..text..val.."</b>" end
    end)
end

-- TABS CONFIG
CreateInput(MoveTab, "Speed: ", 0, 30, function(v) Speed = v end)
CreateInput(MoveTab, "Fly Speed: ", 45, 15, function(v) FlySpeed = v end)
CreateToggle(MoveTab, "BOOST", 90, function(s) Enabled = s end)
CreateToggle(MoveTab, "FLY", 135, function(s) FlyEnabled = s ToggleFly(s) end)

CreateToggle(SpinTab, "SPINBOT", 0, function(s) SpinEnabled = s ToggleSpin(s) end)
CreateInput(SpinTab, "Spin Speed: ", 45, 50, function(v) SpinSpeed = v end)

CreateInput(ViewTab, "FOV: ", 0, 70, function(v) FOVValue = math.clamp(v, 1, 120) end)

CreateToggle(MiscTab, "AUTO STEAL", 0, function(s) AutoSteal = s end)
CreateInput(MiscTab, "Steal Reach: ", 45, 50, function(v) StealDist = v end)
CreateToggle(MiscTab, "ANTI-KB", 90, function(s) AntiKB = s end)

ShowTab("Move")
